#!/usr/bin/env nextflow

/*
 * -------------------------------------------------
 *  iraiosub/riboseq test config file
 * -------------------------------------------------
 * Default parameters config
 */

includeConfig './genomes.config' 

params {
    config_profile_name        = 'riboseq test profile'
    config_profile_description = 'test dataset to check pipeline function'
    // input = '/camp/project/proj-luscombe-ule/working/ira-jure/test_data/samplesheet.csv'
    input = 'https://raw.githubusercontent.com/iraiosub/test_data/main/samplesheet.csv'

    // Reference
    org = "GRCh38"
    // fasta = "/camp/project/proj-luscombe-ule/working/ira-jure/ref/GRCh38.primary_assembly.genome.fa"
    // gtf = "/camp/project/proj-luscombe-ule/working/ira-jure/ref/gencode.v29.primary_assembly.annotation.gtf"
    // star_index = "/camp/project/proj-luscombe-ule/working/ira-jure/ref/star_index"
    // smallrna_fasta = "/camp/lab/ulej/home/users/wilkino/genomes/hs/premapping/named_sn_sno_rRNA.gencode.v29.annotation.plus_oscar.fa"

    // Adapter trimming and length filtering
    skip_preprocessing = false
    adapter_threeprime = null

    // Premapping
    skip_premap = false
    bowtie2_args = '--norc --no-unal --very-sensitive-local -N 1'

    // Mapping

    // Deduplication
    with_umi = true
    skip_umi_extract = true
    umi_separator = 'rbc:'
    skip_qc = false

    // Featurecounts
    strandedness = "forward"

}